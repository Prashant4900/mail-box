# URL of the Next.js app where emails should be forwarded
NEXTJS_APP_URL=http://localhost:3000

# Secret key to authorize webhook requests sent from the worker to the Next.js app
WEBHOOK_SECRET=your_super_secret_webhook_key
